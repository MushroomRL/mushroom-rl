Value functions
===============

The estimators turning a batch of transitions into the target the critic is fitted on, and the advantage the actor
is updated with — the n-step return and the generalized advantage estimator.

.. automodule:: mushroom_rl.rl_utils.value_functions
