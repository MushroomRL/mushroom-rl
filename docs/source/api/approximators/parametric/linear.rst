Linear
======

A linear approximator over the features of the input. The features are applied internally, so the raw state is
passed to ``predict`` and ``fit``, not a pre-computed feature vector.

.. automodule:: mushroom_rl.approximators.parametric.linear
