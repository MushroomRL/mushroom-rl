Recurrent network
=================

The recurrent architecture used with ``RecurrentTorchApproximator``, taking a sequence of observations and
returning the output together with the updated hidden state.

.. automodule:: mushroom_rl.approximators.parametric.networks.recurrent_network
