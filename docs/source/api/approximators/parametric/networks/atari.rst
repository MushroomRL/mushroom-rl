Atari network
=============

The convolutional architecture used on the Atari benchmark, taking a stack of frames as input and returning one
value per action.

.. automodule:: mushroom_rl.approximators.parametric.networks.atari_network
