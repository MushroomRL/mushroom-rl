Torch policy
============

Policies implemented as PyTorch modules, used by the deep algorithms. They expose the distribution they sample from, so
that the algorithms can compute log-probabilities, entropies and reparametrized samples on it.

.. automodule:: mushroom_rl.policy.torch_policy
    :private-members:
