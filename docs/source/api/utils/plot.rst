Result plotting
===============

Matplotlib helpers to plot results *after* a run: they compute the mean and the confidence interval over a set of
runs and draw them as a line with a shaded band, which is the usual way to present a learning curve averaged over
several seeds.

.. automodule:: mushroom_rl.utils.plot
    :private-members:
