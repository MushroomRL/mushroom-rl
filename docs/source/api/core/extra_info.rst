Extra info
==========

``ExtraInfo`` holds everything a dataset stores beside the transitions and keeps its parts in step with each
other: the per-step information an environment returns, what an environment reports when it resets, and the
policy parameters an episodic agent draws.

.. automodule:: mushroom_rl.core.extra_info
