Spaces
======

The observation and action spaces carried by ``MDPInfo``. ``Box`` describes a continuous space through its
``low`` and ``high`` bounds, ``Discrete`` a finite one through the number of values it takes.

.. automodule:: mushroom_rl.core.spaces
