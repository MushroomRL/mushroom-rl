DeepMind Control Suite
======================

Wrapper for the DeepMind Control Suite. The task is given as domain and task name. ``dm_control`` is installed by
the ``mujoco`` extra, so no separate one is needed.

.. automodule:: mushroom_rl.environments.dm_control_env
