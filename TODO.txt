For Mushroom 2.0:
    * add history_length support to SequenceReplayMemory (frame stacking + BPTT simultaneously) and necessary framework level support.
    * fix basis functions
        - vectorize if possible, using vmap if allowed
        - simplify interface, improving facade pattern
    * reorganize example folder
    * linting as much as possible
    * fix input_shape approximator for multiple inputs
    * deterministic policy evaluation


For Mushroom 2.1:
    * vectorize _compute_n_step_return in AbstractReplayMemory
    * fix MellowMax parameter class
    * handle ExtraInfo correctly in ReplayMemory
      - currently ignored in circular-buffer phase; info is never read back during sampling
    * vectorized env rendering
      - render a single window by default instead of one per active env; optionally a grid viewer compositing all envs
